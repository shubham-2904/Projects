const NoteText: React.FC = () => {
    return (
        <h1 className="fixed inset-0 flex justify-center items-center text-9xl font-bold text-[#DADADA] select-none -z-40">
            NOTES
        </h1>
    );
};

export default NoteText;
